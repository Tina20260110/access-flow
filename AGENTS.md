# Access Flow 项目协作指南

## 适用范围

本文件适用于整个仓库。若子目录中存在更具体的 `AGENTS.md`，则该文件仅在对应子目录内补充或覆盖
本指南。所有工作必须同时遵守 `.specify/memory/constitution.md`；发生冲突时，以项目宪章为准。

## 中文规范

- 与用户的沟通、进度更新、问题说明、评审意见和最终总结必须使用简体中文。
- 所有新建或更新的项目文档必须使用简体中文，包括规格、计划、任务、清单、报告和说明文件。
- 所有 `spec.md` 必须使用中文编写，标题、需求、用户故事、验收标准、边界条件和补充说明不得遗漏中文表达。
- 代码注释、测试描述和面向用户的文本默认使用中文；已有约定或外部兼容性要求优先时可以保留原文。
- 源代码标识符、API 字段、命令、文件路径、协议、标准及第三方专有名词可以使用原文，但相关解释必须使用中文。
- 引用外语资料时必须提供中文说明或摘要。确需使用其他语言的交付物，必须记录原因和适用范围。

## Spec Kit 工作流

- 开始规格、计划、任务或实现工作前，先阅读 `.specify/memory/constitution.md` 及当前功能目录下的相关产物。
- 使用 Spec Kit 创建的 `spec.md`、`plan.md`、`tasks.md` 和检查清单必须保持中文语境与术语一致。
- 需求不明确且会实质影响实现方向时，先用中文说明歧义并确认；能够从仓库上下文安全推断时可直接推进。
- 规格、计划与任务之间必须可追溯；实现变更不得偏离已批准的需求和验收标准。

## 实施原则

- 修改前先检查相关文件、现有约定和工作区状态，只改动完成当前任务所需的内容。
- 保留用户已有的改动，不覆盖、不回退与当前任务无关的工作。
- 优先复用现有结构与工具，避免无需求支撑的抽象、依赖和复杂度。
- 代码变更必须补充或更新与风险相称的测试，并运行可用的相关检查。
- 不得掩盖失败的测试、命令或验证结果；无法完成验证时，必须在最终总结中用中文说明原因和风险。

## 交付要求

- 完成工作后检查文档语言是否合规，尤其确认所有 `spec.md` 均为中文。
- 最终总结必须简要说明已完成的变更、验证结果以及仍需关注的事项。
- 命令、路径和代码片段保持原样并使用代码格式，解释文字使用中文。

## 当前项目状态

仓库当前以 Spec Kit 配置和工作流为主，尚未确定应用技术栈及统一的构建、测试命令。引入技术栈后，
必须在本文件中补充准确的环境准备、构建、测试和质量检查命令。
